'use client';
import {useState} from 'react';
export default function Login(){const [role,setRole]=useState('student'); const go=(e:React.FormEvent)=>{e.preventDefault();location.href=`/portal/${role}`};return <main className="min-h-screen grid place-items-center bg-slate-50 p-5"><form onSubmit={go} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-xl border"><h1 className="text-2xl font-bold text-[#0b1f3a]">School Portal</h1><p className="mt-2 text-sm text-slate-500">Demo authentication interface. Connect production auth before deployment.</p><label className="mt-6 block text-sm font-semibold">Role<select value={role} onChange={e=>setRole(e.target.value)} className="mt-2 w-full rounded-lg border p-3">{['student','parent','teacher','admin'].map(x=><option key={x}>{x}</option>)}</select></label><input className="mt-4 w-full rounded-lg border p-3" placeholder="Email" type="email" required/><input className="mt-3 w-full rounded-lg border p-3" placeholder="Password" type="password" required/><button className="mt-5 w-full rounded-lg bg-[#0b1f3a] p-3 font-bold text-white">Sign in</button></form></main>}
